# The JetBrains plugin is built against the IntelliJ Platform, which every JetBrains IDE shares —
# so ONE build covers IntelliJ IDEA, PyCharm, WebStorm, GoLand, RustRover, Rider and Android Studio.
pluginVersion=0.1.32
platformType=IC
platformVersion=2024.2
kotlin.stdlib.default.dependency=false
org.gradle.jvmargs=-Xmx2g
